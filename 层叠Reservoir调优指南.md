# 层叠Reservoir调优指南

## 实验结果分析

### 关键发现
- **5层表现最佳**：NMI=0.8295 (+10.68%), ARI=0.7856 (+17.39%)
- **6层性能下降**：可能是过拟合或配置不当
- **2层表现较差**：特征提取不足

### 性能趋势
```
层数增加 → 性能提升（2→3→4→5）
5层 → 最佳性能
6层 → 性能下降（过拟合风险）
```

## 调优策略

### 1. 单元数配置调优

**当前默认配置**：
- 5层: `[400, 350, 300, 200, 150]`
- 6层: `[400, 350, 300, 250, 200, 150]`

**调优方向**：
- **更宽配置**：增加每层单元数，提升表达能力
  - 示例: `[500, 400, 350, 300, 200]`
- **更窄配置**：减少单元数，降低过拟合风险
  - 示例: `[300, 250, 200, 150, 100]`
- **均匀配置**：所有层使用相同单元数
  - 示例: `[300, 300, 300, 300, 300]`
- **递增配置**：从少到多（较少使用）
  - 示例: `[200, 250, 300, 350, 400]`
- **递减更缓**：保持更多信息
  - 示例: `[400, 380, 360, 340, 320]`

### 2. 表示方法调优

**可选方法**：
- `'mean'`: 所有状态的均值（默认，平衡）
- `'last'`: 最后状态（关注序列末尾）
- `'reservoir'`: 储备池模型空间（更复杂但可能更有效）
- `'output'`: 输出模型空间（需要原始数据）

**建议**：
- 先尝试 `'mean'` 和 `'reservoir'`
- `'last'` 适合关注序列末尾信息的任务
- `'output'` 计算成本较高

### 3. 谱半径调优

**当前默认**: 0.99

**调优范围**：
- **较小值 (0.90-0.95)**：更稳定的动态，但可能表达能力不足
- **默认值 (0.99)**：平衡稳定性和表达能力
- **较大值 (0.999)**：更强的记忆能力，但可能不稳定

**建议**：
- 从0.99开始
- 如果过拟合，降低到0.95
- 如果欠拟合，尝试0.999

### 4. 连接度调优

**当前默认**: 0.3

**调优范围**：
- **稀疏 (0.2-0.3)**：更少的连接，更稳定
- **中等 (0.3-0.4)**：平衡
- **密集 (0.4-0.5)**：更多连接，更强表达能力

**建议**：
- 从0.3开始
- 如果性能不足，尝试0.4
- 如果过拟合，降低到0.2

### 5. 其他超参数

**输入缩放 (input_scaling)**：
- 默认: 0.2
- 调优范围: 0.1-0.5
- 较小值：更稳定，但可能信息不足
- 较大值：更强响应，但可能不稳定

**泄漏率 (leak)**：
- 默认: None（无泄漏）
- 可选: 0.1-0.9
- 泄漏率越小，记忆越短；越大，记忆越长

**噪声水平 (noise_level)**：
- 默认: 0.0
- 调优范围: 0.0-0.1
- 添加噪声可以提高泛化能力，防止过拟合

## 调优流程

### 步骤1：确定最佳层数
运行 `stacked_clustering_comparison.py`，找出最佳层数（当前是5层）

### 步骤2：单元数调优
使用 `stacked_hyperparameter_tuning.py` 测试不同单元数配置

### 步骤3：表示方法调优
在最佳单元数基础上，测试不同表示方法

### 步骤4：谱半径调优
在最佳单元数和表示方法基础上，调整谱半径

### 步骤5：连接度调优
最后微调连接度

### 步骤6：综合验证
使用最佳配置组合，进行最终验证

## 调优脚本使用

### 运行完整调优
```bash
cd examples
python stacked_hyperparameter_tuning.py
```

### 手动调优示例
```python
from reservoir_computing.modules import StackedRC_model

# 自定义配置
reservoir_configs = [
    {
        'n_internal_units': 500,
        'spectral_radius': 0.95,
        'leak': None,
        'connectivity': 0.4,
        'input_scaling': 0.2,
        'noise_level': 0.0,
        'circle': False
    }
    for _ in range(5)  # 5层
]

model = StackedRC_model(
    n_layers=5,
    reservoir_configs=reservoir_configs,
    mts_rep='reservoir',  # 尝试不同表示方法
    readout_type=None
)
```

## 常见问题

### Q1: 为什么6层性能下降？
**A**: 可能原因：
- 过拟合：层数过多，模型过于复杂
- 配置不当：单元数配置不适合6层
- 信息损失：层数过多导致信息逐层衰减

**解决方案**：
- 调整6层的单元数配置（增加单元数）
- 尝试不同的表示方法
- 添加正则化（降维、噪声）

### Q2: 如何防止过拟合？
**A**: 
- 减少层数（5层可能已是最优）
- 降低单元数
- 增加噪声水平
- 使用降维（PCA）
- 尝试更简单的表示方法

### Q3: 如何提升性能？
**A**:
- 增加单元数（但要防止过拟合）
- 尝试 `'reservoir'` 表示方法
- 调整谱半径和连接度
- 尝试不同的单元数配置模式

## 最佳实践

1. **从简单开始**：先使用默认配置，建立基准
2. **逐步调优**：一次只调整一个参数
3. **记录结果**：保存每次调优的结果，便于对比
4. **验证泛化**：在不同数据集上验证最佳配置
5. **平衡性能**：考虑计算成本和性能提升的平衡

## 预期改进

基于当前结果（5层最佳），通过调优可能达到：
- **NMI**: 0.85+ (当前0.8295)
- **ARI**: 0.80+ (当前0.7856)

关键调优方向：
- 优化单元数配置
- 尝试 `'reservoir'` 表示方法
- 微调谱半径和连接度
