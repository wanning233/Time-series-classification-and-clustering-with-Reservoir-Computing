# 层叠Reservoir调优分析报告

## 1. 项目概述

本项目基于Reservoir Computing（储备池计算）架构，专注于时间序列分类和聚类任务。核心创新点在于引入了**层叠Reservoir（StackedReservoir）**架构，通过多层Reservoir串联，逐层提取更高层次的时间序列特征。

### 关键组件
- **RC_model**: 单层Reservoir模型（基线）
- **StackedRC_model**: 层叠Reservoir模型（核心创新）
- **实验脚本**: 提供完整的调优和对比功能

## 2. 当前实验结果分析

### 2.1 层数对比结果
基于Japanese_Vowels数据集的聚类实验结果：

| 层数 | NMI | ARI | 相对基线提升 |
|------|-----|-----|-------------|
| 单层 | 0.75 | 0.67 | 基准 |
| 2层 | 0.73 | 0.65 | -2.7% / -3.0% |
| 3层 | 0.79 | 0.72 | +5.3% / +7.5% |
| 4层 | 0.81 | 0.76 | +8.0% / +13.4% |
| **5层** | **0.83** | **0.79** | **+10.7% / +17.9%** |
| 6层 | 0.80 | 0.75 | +6.7% / +11.9% |

**关键结论**：
- **5层架构表现最佳**，NMI提升10.7%，ARI提升17.9%
- 层数过多（6层）会导致性能下降，存在过拟合风险
- 层数过少（2层）特征提取不足

### 2.2 超参数调优结果

#### 单元数配置调优
测试了6种不同配置，最佳结果：
- **默认配置**：[400, 350, 300, 200, 150]
- **更宽配置**：[500, 400, 350, 300, 200]（推荐）
- 其他配置表现：更窄配置、均匀配置、递增配置、递减更缓配置

#### 表示方法调优
- **mean**（均值）：平衡选择
- **reservoir**（储备池模型空间）：最佳选择
- **last**（最后状态）：适合关注序列末尾的任务
- **output**（输出模型空间）：计算成本高

#### 谱半径调优
- **0.99**：整体稳健选择
- 0.95：有助于抑制过拟合
- 0.999：增强记忆能力，但可能不稳定

#### 连接度调优
- **0.3**：稳健折中选择
- 0.4：某些设置下有小幅提升
- 0.5：计算量显著增加，过拟合风险

## 3. Kaggle运行调优建议

### 3.1 推荐调优策略

#### 策略1：宽配置测试（当前exp_002）
```python
units_per_layer = [500, 400, 350, 300, 200]  # 比默认配置更宽
spectral_radius = 0.99
connectivity = 0.3
mts_rep = 'reservoir'  # 最佳表示方法
```

**预期结果**：
- NMI > 0.83（当前0.8295）
- ARI > 0.78（当前0.7856）

#### 策略2：精细调优
在宽配置基础上：
1. 调整谱半径（0.95-0.999）
2. 微调连接度（0.2-0.5）
3. 测试不同噪声水平（0.0-0.1）

#### 策略3：降维优化
- 尝试PCA降维（dimred_method='pca'）
- 调整降维维度（n_dim）

### 3.2 Kaggle运行步骤

1. **准备环境**：
   - 确保Internet开启
   - 选择合适的加速器（GPU推荐）

2. **运行exp_002**：
   ```bash
   # 本地生成Kaggle Notebook
   python generate_experiment.py --exp exp_002 --output kaggle_exp_002.ipynb
   ```

3. **监控运行**：
   - 预计运行时间：10-30分钟
   - 监控内存和GPU使用情况

4. **结果分析**：
   - 对比基准结果（NMI=0.8295）
   - 分析不同参数组合的效果

### 3.3 调优脚本使用

#### 运行完整调优
```bash
cd examples
python stacked_hyperparameter_tuning.py
```

#### 自定义调优
```python
from reservoir_computing.modules import StackedRC_model

# 宽配置示例
reservoir_configs = [
    {
        'n_internal_units': 500,
        'spectral_radius': 0.99,
        'leak': None,
        'connectivity': 0.3,
        'input_scaling': 0.2,
        'noise_level': 0.0,
        'circle': False
    }
    for _ in range(5)  # 5层
]

model = StackedRC_model(
    n_layers=5,
    reservoir_configs=reservoir_configs,
    mts_rep='reservoir',
    readout_type=None
)
```

## 4. 预期改进方向

### 4.1 短期改进（基于当前结果）
- **NMI目标**：0.85+（当前0.8295）
- **ARI目标**：0.80+（当前0.7856）

### 4.2 关键调优方向
1. **单元数配置**：尝试更宽配置[500, 400, 350, 300, 200]
2. **表示方法**：优先测试'reservoir'表示
3. **谱半径**：精细调优0.95-0.999
4. **连接度**：尝试0.2-0.5范围
5. **降维**：测试PCA降维效果

### 4.3 风险与注意事项
- **过拟合风险**：层数过多或单元数过大
- **计算成本**：宽配置需要更多计算资源
- **调参成本**：需要系统性的实验设计

## 5. 实验建议

### 5.1 实验设计
1. **基准测试**：运行当前默认配置
2. **宽配置测试**：exp_002（当前推荐）
3. **精细调优**：基于宽配置进一步优化
4. **验证泛化**：在不同数据集上验证

### 5.2 监控指标
- **NMI（标准化互信息）**：衡量聚类质量
- **ARI（调整兰德指数）**：衡量聚类一致性
- **聚类数**：实际聚类数量
- **训练时间**：计算效率

### 5.3 文档更新建议
1. 更新实验结果到`层叠Reservoir实验总结.md`
2. 记录新的最佳配置和结果
3. 更新调优指南中的推荐参数

## 6. 结论

层叠Reservoir架构在时间序列聚类任务中展现出显著优势：
- **5层架构**是当前最佳配置
- **宽配置**（更大学单元数）有提升潜力
- **reservoir表示方法**值得优先尝试
- **系统调优**可以进一步提升性能

建议先在Kaggle上运行exp_002宽配置测试，然后基于结果进行精细调优。通过合理的参数组合，有望将NMI提升到0.85+，ARI提升到0.80+。

---
*报告基于项目现有文档和代码分析，具体调优效果需要实际实验验证。*