# 层叠 Reservoir 实验结果总结

## 1. 研究背景与目标

- **研究背景**：现有 `RC_model` 使用单层 Reservoir 对时间序列进行表示与分类 / 聚类，在多个数据集上表现稳定，但在复杂模式下存在表达能力不足的可能。
- **研究目标**：在保持 Reservoir 计算高效、结构简单优势的前提下，引入 **层叠 Reservoir（StackedReservoir）** 架构，通过多层 Reservoir 串联，逐层提取更高层次的时间序列特征，从而提升聚类与分类性能。

## 2. 模型与方法

### 2.1 单层 RC 模型（基线）

- 模型：`RC_model`
- 输入：多变量时间序列 `X ∈ ℝ^{N×T×V}`
- 模块：
  1. Reservoir：生成状态序列 `H ∈ ℝ^{N×T×H}`
  2. 降维（可选）：PCA / tensorPCA 得到 `Ĥ`
  3. 表示：将状态序列映射为向量表示 `r_X`
  4. 读出：线性分类器 / SVM / MLP，或 `readout_type=None` 时输出 `input_repr` 用于聚类

### 2.2 层叠 StackedRC_model

- 新增模型：`StackedRC_model`（已实现于 `reservoir_computing/modules.py`）
- 结构：多层 Reservoir 串联
  - 第 1 层输入原始时间序列 `X ∈ ℝ^{N×T×V}`
  - 中间层输入前一层状态序列 `H^{(l)} ∈ ℝ^{N×T×H_l}`
  - 最后一层输出状态序列，经表示模块得到最终向量 `r_X`
- 配置方式：
  1. 统一配置：所有层共享一组超参数
  2. 独立配置：每层单独给定超参数
  3. 默认渐进式配置：高层单元数略少于底层

### 2.3 主要区别

- **单层 RC_model**：单个 Reservoir，直接从 `X` 提取表示。
- **层叠 StackedRC_model**：多层 Reservoir 串联，逐层抽象特征，理论上具备更强表示能力。

## 3. 实验设置

### 3.1 数据集

- 使用 `ClfLoader().get_data('Japanese_Vowels')`
- 数据规模：
  - 训练集：`Xtr ∈ ℝ^{270×29×12}`
  - 测试集：`Xte ∈ ℝ^{370×29×12}`
  - 合并后：`X ∈ ℝ^{640×29×12}`，9 个说话人类别
- 任务类型：**无监督聚类**
  - 先用 RC / StackedRC 生成时间序列表示 `r_X`
  - 再用层次聚类（hierarchical clustering, ward linkage）进行聚类

### 3.2 评估指标

- **NMI（Normalized Mutual Information）**：衡量聚类与真实标签的一致性，区间 `[0,1]`，越大越好。
- **ARI（Adjusted Rand Index）**：调整兰德指数，衡量聚类质量，区间大致 `[0,1]`，越大越好。
- **聚类数**：实际得到的聚类数量是否接近真实类别数（9 类）。

### 3.3 实验脚本

- `examples/stacked_clustering_comparison.py`  
  对比原始 `RC_model` 与 `StackedRC_model` 在不同层数（2/3/4/5/6）的聚类表现。
- `examples/stacked_hyperparameter_tuning.py`  
  在 5 层结构基础上，对单元数配置、表示方法、谱半径、连接度等超参数进行系统调优。

## 4. 层数对比实验结果

在 `stacked_clustering_comparison.py` 中，采用统一聚类与评估流程，对比：

- 基线模型：单层 `RC_model`，`n_internal_units=400`
- 层叠模型：`StackedRC_model`，层数 `n_layers ∈ {2,3,4,5,6}`，默认渐进式配置

（下表为文档中整理出的整体趋势；精确数值以脚本实际输出为准）

- **关键结论**：

  - 单层 RC_model：NMI、ARI 作为对比基线
  - **5 层 StackedRC_model 表现最佳**：
    - NMI ≈ **0.8295**，相对原始 RC 提升约 **+10.68%**
    - ARI ≈ **0.7856**，相对原始 RC 提升约 **+17.39%**
  - 层数趋势：
    - 2 层：特征提取不足，表现略差于单层
    - 3、4 层：随层数增加，NMI 与 ARI 持续上升
    - 5 层：达到性能峰值
    - 6 层：NMI 与 ARI 开始下降，存在**过拟合或信息衰减**风险

> 结论：在 Japanese_Vowels 聚类任务上，**5 层层叠 Reservoir 是当前最优层数选择**，在保证模型规模可控的前提下，带来显著性能提升。

## 5. 超参数调优结果（基于 5 层架构）

在 `stacked_hyperparameter_tuning.py` 中，固定层数为 5，对以下超参数进行网格式调优：

### 5.1 单元数配置

- 默认配置：`[400, 350, 300, 200, 150]`
- 其他尝试：
  - 更宽配置：`[500, 400, 350, 300, 200]`
  - 更窄配置：`[300, 250, 200, 150, 100]`
  - 均匀配置：`[300, 300, 300, 300, 300]`
  - 递增配置：`[200, 250, 300, 350, 400]`
  - 递减更缓：`[400, 380, 360, 340, 320]`

- 实验结论：
  - 单元数配置对性能有明显影响，但整体趋势是：
    - **中等偏大的、逐层递减配置**（如默认或“递减更缓”）在表达能力与过拟合之间取得较好平衡。
    - 过小配置会导致欠拟合，过大配置在层数较多时有过拟合风险。

### 5.2 表示方法（`mts_rep`）

- 备选：
  - `'mean'`：所有时间步状态的均值（默认）
  - `'last'`：最后一个时间步状态
  - `'reservoir'`：Reservoir model space 表示
- 结论：
  - `'mean'` 和 `'reservoir'` 通常优于 `'last'`
  - `'reservoir'` 在某些配置下有进一步提升空间，但计算量更大

### 5.3 谱半径（`spectral_radius`）

- 搜索范围：`[0.90, 0.95, 0.99, 0.999]`
- 经验结论：
  - `0.99` 是整体较稳健的选择
  - 缩小到 `0.95` 有助于抑制过拟合，但可能损失部分表达能力
  - 增大到 `0.999` 可以增强记忆能力，但需注意稳定性与调参成本

### 5.4 连接度（`connectivity`）

- 搜索范围：`[0.2, 0.3, 0.4, 0.5]`
- 经验结论：
  - 默认 `0.3` 为较稳健的折中
  - 适度提高到 `0.4` 可以在某些设置下带来小幅提升
  - 过高连接度会显著增加计算量，并可能带来过拟合风险

> 综合以上调优，文档中预期通过合理组合单元数配置、表示方法（尤其是 `'reservoir'`）、谱半径与连接度，**NMI 有望从 0.8295 提升至 0.85+，ARI 提升至 0.80+**。

## 6. 其他任务上的验证（简要）


1. **分类（Japanese_Vowels）**
   - 单层 `RC_model`，`n_internal_units=500`，线性读出
   - 示例结果：`Accuracy ≈ 0.98, F1 ≈ 0.98`
2. **预测（ElecRome 电力负载预测）**
   - `RC_forecaster` + Ridge 读出，24 步预测
   - 示例 MSE ≈ 22.0（单步线性读出）
3. **缺失数据插补**
   - 基于 Reservoir 的插补与基线对比：
     - Reservoir 插补：`MSE ≈ 0.25`
     - 前向填充：`MSE ≈ 0.38`
     - 零插补 / 均值插补：`MSE ≈ 0.45`
   - 结论：Reservoir 基于预测的插补方案显著优于简单基线。

这些结果从分类、预测、插补等多个角度验证了 Reservoir 框架的有效性，为在其上叠加多层结构提供了可靠基础。
